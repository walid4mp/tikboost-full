# TikBoost — إصلاحات 18-08-2026

تم إصلاح المشاكل التي ظهرت بعد تفعيل إعلان الصورة، إضافة إلى لوحة الإدارة واستعادة كلمة المرور.

## ما تم إصلاحه

- إعلان الصورة المخصص لم يعد يستطيع تعطيل Banner AdMob عند وجود رابط صورة خاطئ أو صورة غير متاحة.
- إذا فشل تحميل صورة الإعلان، التطبيق يرجع تلقائيًا إلى Banner AdMob بدل عرض مساحة فارغة أو التسبب في مشكلة في الواجهة.
- روابط صور وإعلانات الإدارة أصبحت تُقبل كـ HTTP/HTTPS فقط.
- رابط الضغط على الإعلان يتم التحقق منه قبل فتحه.
- لوحة الإدارة أصبحت RTL بشكل صحيح، والـ sidebar على اليمين، مع تخطيط Responsive للهاتف والكمبيوتر.
- الحقول والجداول في لوحة الإدارة لم تعد توسّع الشاشة أفقيًا.
- رمز استعادة كلمة المرور أصبح يظهر بعد زر «إظهار الرمز» مع:
  - نسخ الرمز.
  - مشاركة الرمز من Web Share API عند توفرها.
  - fallback للنسخ حتى عندما لا يعمل `navigator.clipboard`.
- استعادة كلمة المرور أصبحت ترسل رمز OTP بالبريد تلقائيًا عندما يكون مزود البريد مضبوطًا، مع بقاء كشف الرمز من الإدارة كخطة بديلة.
- مدة صلاحية OTP أصبحت تستخدم `PASSWORD_RESET_TTL_MIN` بدل قيمة ثابتة.
- إصلاح تسجيل حدث كشف OTP في سجلات الإدارة ليشير إلى المستخدم الصحيح.

## مهم قبل الإنتاج

في Render يجب ضبط متغيرات البريد الحقيقية:

- `EMAIL_PROVIDER=resend` أو مزود مدعوم آخر.
- `EMAIL_API_KEY` = مفتاح API الحقيقي للمزود.
- `MAIL_FROM` = عنوان إرسال موثّق لدى مزود البريد.
- `PASSWORD_RESET_TTL_MIN=10`
- `RESET_OTP_ENCRYPTION_KEY` = سر طويل ثابت. لا تغيّره بعد وجود طلبات استعادة قائمة، لأن الطلبات القديمة مشفرة بهذا السر.

إذا لم يكن مزود البريد جاهزًا، طلب الاستعادة سيظل يُنشأ ويمكن للمدير كشف الرمز يدويًا من صفحة «استعادة كلمات المرور».

## فحص تم إجراؤه

تم فحص جميع ملفات JavaScript الخاصة بالـ backend ولوحة الإدارة بواسطة `node --check` ولم تظهر أخطاء Syntax.

بيئة التنفيذ الحالية لا تحتوي على Flutter SDK، لذلك لم يتم إنشاء APK/AAB محليًا داخل هذه الجلسة. مشروع Flutter نفسه بقي ضمن الحزمة وجاهز للبناء على بيئة Flutter/CI.
